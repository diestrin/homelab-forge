# factory/plans

Short plan markdown files paired with `planning` tasks (ADR-009). The Slack
orchestrator writes `TASK-NNN.md` here for plan PR bodies. Not a second SoT —
task YAML under `../tasks/` remains authoritative; plans are narrative for review.
