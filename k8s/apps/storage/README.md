# Object storage — Garage (ADR-015)

Operator runbook: [`docs/runbooks/storage.md`](../../../docs/runbooks/storage.md).

Argo CD Application `storage` syncs this directory from `main`. Do not
`kubectl apply` it.
