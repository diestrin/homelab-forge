apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: github-actions-runner-data
  namespace: forge-ci
  labels:
    app: github-actions-runner
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
