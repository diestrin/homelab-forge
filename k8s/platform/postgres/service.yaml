apiVersion: v1
kind: Service
metadata:
  name: forge-postgres
  labels:
    app: forge-postgres
spec:
  type: ClusterIP
  selector:
    app: forge-postgres
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
